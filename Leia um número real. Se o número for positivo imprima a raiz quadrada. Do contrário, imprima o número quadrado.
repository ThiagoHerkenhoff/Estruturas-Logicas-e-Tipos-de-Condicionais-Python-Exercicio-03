# 3 - Leia um número real. Se o número for positivo imprima a raiz quadrada. Do contrário,
# imprima o número quadrado.

import math

n = float(input('Informe um número: '))

if n > 0:
    raiz = math.sqrt(n)
    print(f'A raiz quadrada do número {n} é {raiz}')

else:
    q = n ** 2
    print(f'O quadrado do número {n} é {q}')
